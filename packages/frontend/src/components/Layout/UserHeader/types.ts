/* eslint-disable @typescript-eslint/no-empty-object-type */

/**
 * UserHeader 组件 Props 类型
 * 当前无 props，预留扩展
 */
export interface IProps {}

/**
 * UserHeader 组件 Emits 类型
 * 当前无事件，预留扩展
 */
export type IEmits = {
  (e: 'back'): void;
}
